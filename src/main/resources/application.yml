spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/yuzhouwudiyyc/springcloud-config.git

      label: master
server:
  port: 7755